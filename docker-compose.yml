version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: hanvault_pg
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ldmpq11@
      POSTGRES_DB: hanvault_db
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d hanvault_db"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata: